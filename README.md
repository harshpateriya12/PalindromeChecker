USE CASE: 01 System Initialization CONCEPTS USED: Class – Acts as a container for the Palindrome Checker application logic, Main Method – Entry point of the Java application with the signature public static void main(String[] args), Static Keyword – Allows the JVM to invoke the main() method without creating an object, Console Output – System.out.println() is used to display messages on the console, Application Flow Control – Defines the startup behavior before palindrome processing begins.
